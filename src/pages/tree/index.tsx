import React from 'react';
import Page from '@/components/common/page';
import TreeBody from '@/components/tree';

const Tree: React.FC = () => {
    return (
        <Page>
            <TreeBody/>
         </Page>
    );
};

export default Tree;
